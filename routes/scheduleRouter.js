let express = require("express");
let router = express.Router();



/* GET current week for specific user */
router.get("/", function (req, res, next) {
  Event
  res.json({});
});

module.exports = router;
